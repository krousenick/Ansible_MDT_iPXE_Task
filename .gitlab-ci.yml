stages:
  - lint
  - deploy

variables:
  DEPLOY_PATH: "F:/wwwroot"

ipxe-lint:
  stage: lint
  image: ubuntu:latest
  before_script:
    - apt-get update && apt-get install -y --no-install-recommends > /dev/null 2>&1
  script:
    - chmod +x scripts/lint_ipxe.sh
    - ./scripts/lint_ipxe.sh
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH
    - if: $CI_COMMIT_TAG

kickstart-lint:
  stage: lint
  image: fedora:44
  before_script:
    - dnf install -y pykickstart > /dev/null 2>&1
  script:
    - chmod +x scripts/lint_kickstart.sh
    - ./scripts/lint_kickstart.sh
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH
    - if: $CI_COMMIT_TAG

theme-lint:
  stage: lint
  image: fedora:44
  before_script:
    - dnf install -y python3 glib2 gtk4 sassc > /dev/null 2>&1
  script:
    - chmod +x scripts/lint_themes.sh
    - ./scripts/lint_themes.sh
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH
    - if: $CI_COMMIT_TAG

deploy-staging:
  stage: deploy
  image: python:3.12-alpine
  before_script:
    - apk add --no-cache openssh-client gettext openssl bash
    - chmod +x scripts/*.sh
    - source scripts/setup_ssh.sh "$STAGING_SSH_HOST" "$STAGING_SSH_USER" "$STAGING_SSH_KEY"
  script:
    - bash scripts/deploy.sh
  environment:
    name: staging
    url: http://$STAGING_SSH_HOST
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
  when: manual
  needs:
    - ipxe-lint
    - kickstart-lint

deploy-production:
  stage: deploy
  image: python:3.12-alpine
  before_script:
    - apk add --no-cache openssh-client gettext openssl bash
    - chmod +x scripts/*.sh
    - source scripts/setup_ssh.sh "$PROD_SSH_HOST" "$PROD_SSH_USER" "$PROD_SSH_KEY"
  script:
    - bash scripts/deploy.sh
  environment:
    name: production
    url: http://$PROD_SSH_HOST
  rules:
    - if: $CI_COMMIT_TAG
  needs:
    - ipxe-lint
    - kickstart-lint

download-fedora-iso:
  stage: deploy
  image: ubuntu:latest
  before_script:
    - apt-get update && apt-get install -y --no-install-recommends curl openssh-client p7zip-full ca-certificates > /dev/null 2>&1
    - chmod +x scripts/*.sh
    - source scripts/setup_ssh.sh "$PROD_SSH_HOST" "$PROD_SSH_USER" "$PROD_SSH_KEY"
  script:
    - bash scripts/download_fedora.sh
  environment:
    name: production
    url: http://$PROD_SSH_HOST
  rules:
    - if: $FEDORA_DOWNLOAD == "true"
  when: manual

build-ipxe:
  stage: deploy
  image: ubuntu:latest
  before_script:
    - apt-get update && apt-get install -y --no-install-recommends build-essential libssl-dev liblzma-dev git openssh-client ca-certificates > /dev/null 2>&1
    - chmod +x scripts/*.sh
    - source scripts/setup_ssh.sh "$PROD_SSH_HOST" "$PROD_SSH_USER" "$PROD_SSH_KEY"
  script:
    - bash scripts/build_ipxe.sh
  environment:
    name: production
    url: http://$PROD_SSH_HOST
  rules:
    - if: $IPXE_BUILD == "true"
  when: manual